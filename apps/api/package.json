{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "uv sync",
    "dev": "uv run python -X utf8 dev.py",
    "start": "uv run python -X utf8 -m fastapi run app/main.py"
  }
}
